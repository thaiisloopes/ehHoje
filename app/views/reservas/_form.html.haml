= simple_form_for(@reserva) do |f|
  = f.error_notification

  .form-inputs
    = f.input :Data
    = f.input :Hora
    = f.association :cliente
    = f.association :evento

  .form-actions
    = f.submit "Salvar"
